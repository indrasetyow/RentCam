<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentcam">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_kamera1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_kamera1"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentCan">
        <activity android:name=".CetakSewaActivity" />
        <activity android:name=".Penyewa1Activity" />
        <activity android:name=".UbahPenyewaActivity" />
        <activity android:name=".LihatPenyewaActivity" />
        <activity android:name=".TambahPenyewaActivity" />
        <activity android:name=".RentalActivity" />
        <activity android:name=".DetailKameraActivity" />
        <activity android:name=".SewaKameraActivity" />
        <activity android:name=".DataKameraActivity" />
        <activity
            android:name=".SplaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>